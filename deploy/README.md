# Deployment

This project has two deployed services:

- `frontend/` runs on Vercel.
- `backend/` runs on Render.

Keep platform-specific notes in this folder, but keep required platform config files in the locations where the platforms can read them.
