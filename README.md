# Hollow Fall

Project layout:

- `frontend/` - TanStack Start website deployed on Vercel.
- `backend/` - FastAPI chatbot/RAG backend deployed on Render.
- `deploy/` - deployment setup notes for Vercel and Render.
- `scripts/` - local helper scripts.

Run locally:

```powershell
npm run start:dev
```
